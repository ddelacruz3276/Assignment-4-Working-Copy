@model DCOEC.Models.Variety
@using Microsoft.AspNetCore.Http


@{
    ViewData["Title"] = "Add a {" + ViewData["CropName"]+"} variety";
}


<h2>Add a {@ViewData["CropName"]} variety</h2>



<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CropId" value="ViewBag.cId"/>
            
            
                        @*<div class="form-group" readonly="readonly">
                    <label asp-for="CropId" readonly="readonly" class="control-label"></label>
                    <select asp-for="CropId" readonly="readonly" class ="form-control" asp-items="ViewBag.CropId"></select>

                    <select id="EmployeeId" name="EmployeeId"  multiple="multiple" asp-items="@ViewBag.Employees">
            <option>Please select</option>
        </select>

                </div>*@
            
            <input type="hidden" name="CropId" value="@ViewBag.cId" />

            @*<select id="CropId" name="CropId" asp-items="@ViewBag.CropId">
                <option>Please select</option>
            </select>*@

            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
